#ifndef _DATE_H
#define _DATE_H

#include <iostream>
class Date{
	public:
	void addDay(int);
	void addYear(int);
	void addMonth(Mese);

	Date();
	Date(int, Mese, int);
	
	private:
	int day{1};
	int year {1979};
	enum Mese{gen,feb,mar,apr,mag,giu,lug,ago,set,ott,nov,dic};
};

#endif
